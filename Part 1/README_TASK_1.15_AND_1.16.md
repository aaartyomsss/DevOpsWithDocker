## Task 1.15

As an example this project could be run either each and every image could be run separately the same way as in the tasks above or one could user `docker-compose up --build` running it from the root directory

## Task 1.16
